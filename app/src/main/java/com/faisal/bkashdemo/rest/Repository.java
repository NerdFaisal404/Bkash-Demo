package com.faisal.bkashdemo.rest;

import android.graphics.Bitmap;
import android.media.MediaMetadataRetriever;
import android.support.annotation.NonNull;
import android.util.Log;


import org.json.JSONObject;

import java.io.File;
import java.util.ArrayList;
import java.util.List;

import io.reactivex.Flowable;
import okhttp3.MediaType;
import okhttp3.MultipartBody;
import okhttp3.RequestBody;
import okhttp3.ResponseBody;

public class Repository {

    private static final String TAG = "Repository";


  /*  //Delete Forum
    public static Flowable<JSONObject> deletePost(String postId) {
        Endpoints apiInterface = RetrofitSingleton.getInstance().create(Endpoints.class);
        return apiInterface.deletePost(postId);
    }

    //Delete Reply
    public static Flowable<JSONObject> deleteReply(String postId) {
        Endpoints apiInterface = RetrofitSingleton.getInstance().create(Endpoints.class);
        return apiInterface.deleteReply(postId);
    }*/



}
